# 🧠 Нейро-символический решатель логических задач

Гибридная система, которая комбинирует **LLM** (понимание русского языка) и **метод резолюций** (строгое математическое доказательство) для решения логических задач.

## 🎯 Что это?

**3 модуля в одной системе:**

1. **Формализатор** — LLM преобразует задачу на русском в формулы логики
2. **Резолюция** — математический движок доказывает формулы (100% надёжно)
3. **Объяснятор** — LLM объясняет доказательство снова на русском

**Пример:**
```
Входная задача: "Все люди смертны. Сократ — человек. Является ли Сократ смертным?"
↓
Формулы: ∀x (Человек(x) → Смертен(x)), Человек(Сократ)
↓
Результат: ✅ ДОКАЗАНО (через метод резолюций)
↓
Объяснение: "Мы знаем, что все люди смертны. Сократ — это человек. 
             Следовательно, Сократ смертен."
```

---

## 🚀 Быстрый старт

### Требования
- Python 3.8+
- API ключ для Perplexity AI (`https://www.perplexity.ai/settings/labs`)

### Установка

1. **Клонируйте репо:**
```bash
git clone https://github.com/bermaxim/neuro-symbolic-reasoner.git
cd neuro-symbolic-reasoner
```

2. **Создайте виртуальное окружение:**
```bash
python -m venv venv
source venv/bin/activate  # macOS/Linux
# или на Windows: venv\Scripts\activate
```

3. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

4. **Создайте файл `.env`:**
```bash
echo 'PERPLEXITY_API_KEY=your-api-key-here' > .env
```

Вставьте свой API ключ из Perplexity AI.

5. **Запустите сервер:**
```bash
python app.py
```

6. **Откройте браузер:**
```
http://localhost:5009
```

---

## 💡 Примеры использования

### Пример 1: Сократ
```
Все люди смертны.
Сократ — человек.
Является ли Сократ смертным?
```
**Результат:** ✅ ДОКАЗАНО

### Пример 2: Вася и стипендия
```
Все студенты, сдающие матан, получают стипендию.
Каждый, получающий стипендию, доволен жизнью.
Вася сдаёт матан.
Докажи: Вася доволен жизнью.
```
**Результат:** ✅ ДОКАЗАНО (5 шагов резолюции)

---

## 📁 Структура проекта

```
mlita/
├── app.py                    # Flask сервер + модули 1 и 3 (LLM)
├── resolution_engine.py      # Модуль 2 (метод резолюций)
├── logic_ast.py              # Парсер и преобразования формул
├── requirements.txt          # Зависимости
├── templates/
│   └── index.html            # Веб-интерфейс
└── static/
    ├── css/style.css         # Стили
    └── js/app.js             # JavaScript
```

---

## 🔧 API

### POST `/api/solve`

**Запрос:**
```json
{
  "task": "Все люди смертны. Сократ — человек. Смертен ли Сократ?"
}
```

**Ответ:**
```json
{
  "formulas": [
    "∀x (Человек(x) → Смертен(x))",
    "Человек(Сократ)"
  ],
  "goal": "Смертен(Сократ)",
  "proven": true,
  "proof_steps": ["[КНФ] ...", "[Резолюция] ..."],
  "explanation": "Мы знаем, что все люди смертны..."
}
```

---

## 🛠️ Как это работает

### Шаг 1: Формализация (LLM)
Пользовательский текст → Формулы логики предикатов
```
Все люди смертны → ∀x (Человек(x) → Смертен(x))
```

### Шаг 2: Резолюция (Математика)
Формулы → Преобразование в КНФ → Метод резолюций → Поиск пустой клаузы

### Шаг 3: Объяснение (LLM)
Логические шаги → Объяснение на русском
