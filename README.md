🧠 Нейро-символический решатель логических задач
Hybrid AI System: LLM + Symbolic Logic for Formal Problem Solving

Система, которая комбинирует мощь больших языковых моделей (LLM) с математической строгостью символической логики для решения логических задач на русском языке.

📋 Описание проекта
Суть идеи

Решение логических задач требует двух типов интеллекта:

Интуитивный — понимание естественного языка

Строгий — формальные доказательства без ошибок

Наша система разделяет эти функции между тремя модулями:

text
Текстовая задача на русском (от пользователя)
                ↓
    [Модуль 1: LLM-формализатор]
    Преобразует текст в формулы логики
                ↓
   Формализованные формулы логики предикатов
                ↓
    [Модуль 2: Движок резолюций]
    Математическое доказательство методом резолюций
                ↓
   Пошаговое доказательство (лог резолюции)
                ↓
    [Модуль 3: LLM-объяснятор]
    Преобразует логику обратно в понятный русский текст
                ↓
   Объяснение на русском языке (для пользователя)
Почему это работает

LLM понимает естественный язык лучше, чем трудно-настраиваемые парсеры

Метод резолюций 100% надёжен — гарантирует корректность доказательства

Разделение ответственности — если LLM ошибётся при формализации, резолюция всё равно будет работать правильно для полученной формулы

🏗️ Архитектура проекта
Текущая структура

text
mlita/
├── app.py                    # Главное Flask приложение
├── resolution_engine.py      # Движок резолюций (модуль 2)
├── logic_ast.py              # AST и парсер формул (модуль 1 + 2)
├── requirements.txt          # Зависимости
│
├── templates/
│   └── index.html            # Веб-интерфейс
│
└── static/
    ├── css/style.css         # Стили
    └── js/app.js             # JavaScript логика
Три основных модуля

Модуль 1: LLM-формализатор (app.py: formalize_task)

Что делает: Преобразует текст на русском в формулы логики предикатов

Пример:

text
Вход:  "Все люди смертны. Сократ — человек."
Выход: 
  - ∀x (Человек(x) → Смертен(x))
  - Человек(Сократ)
Модуль 2: Движок резолюций (resolution_engine.py)

Что делает: Строгое математическое доказательство методом резолюций

Процесс:

Преобразует формулы в КНФ (конъюнктивная нормальная форма)

Добавляет отрицание цели

Применяет резолюцию с унификацией

Ищет пустую клаузу (противоречие = доказательство)

Почему надёжно: Метод резолюций полный и корректный для логики первого порядка

Модуль 3: LLM-объяснятор (app.py: explain_proof_with_api)

Что делает: Преобразует пошаговое доказательство в объяснение на русском

git remote add origin git@github.com:bermaxim/neuro-symbolic-reasoner.git